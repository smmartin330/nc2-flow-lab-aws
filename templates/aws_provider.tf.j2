terraform {
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 6.0"
    }
  }
}

provider "aws" {
  access_key = "{{ access_key }}"
  secret_key = "{{ access_secret }}"
  token      = "{{ token }}"
}
